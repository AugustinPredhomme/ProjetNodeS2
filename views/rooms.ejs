<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Debug - Rooms (<%= dataCount %>)</title>
</head>
<body>
  <h1>Room Data (<%= dataCount %> records)</h1>
  <% if (data.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>Room Number</th>
          <th>Room Type</th>
          <th>Max Occupancy</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% for (const room of data) { %>
          <tr>
            <td><%= room.roomNumber %></td>
            <td><%= room.roomType %></td>
            <td><%= room.maxOccupancy %></td>
            <td><%= room.price %></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  <% } else { %>
    <p>No Room data found.</p>
  <% } %>
</body>
</html>